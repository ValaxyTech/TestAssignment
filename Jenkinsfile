pipeline {
    agent { label 'master' }
     environment {

          }
    stages {
        stage('Checking .NET core Version') {
            steps {
                sh 'dotnet --version'
            }
        }

        stage('Build') {
            
            steps {
  
                }
            }
    }
}
